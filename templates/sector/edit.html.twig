{% extends 'admin_base.html.twig' %}
{% block content %}
<div class="container">
    <h1>Édition du secteur {{sector}}</h1>

    {{ include('sector/_form.html.twig') }}

    <div class="clear1"></div>
    <a href="{{ path('sector_index') }}" class="btn btn-primary">
    	<i class="fa fa-arrow-left"></i>
    	Retour à la liste
    </a>
    <div class="clear1"></div>
    {{ include('sector/_delete_form.html.twig') }}
</div>
{% endblock %}