{% extends 'admin_base.html.twig' %}

{% block content %}
    <div class="container">
        <h1>OrganizationType</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ organization_type.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ organization_type.name }}</td>
                </tr>
                <tr>
                    <th>Slug</th>
                    <td>{{ organization_type.slug }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('organization_type_index') }}">back to list</a>

        <a href="{{ path('organization_type_edit', {'id': organization_type.id}) }}">edit</a>

        {{ include('organization_type/_delete_form.html.twig') }}
    </div>
{% endblock %}